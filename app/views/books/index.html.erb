<h1>Books</h1>
<%= @books.each do |bk| %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
    <tr>
      <tb><span><%= bk.title %></span></tb>
      <tb><span><%= bk.body %></span></tb>
      <tb><a 


  </thead>
<% end %>
<h3>New book</h3>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>